using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using DG.Tweening;
public class Tweeing : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        transform.DOMove(new Vector3(-1291.95f, 0.97f, 2171.213f), 2f);
    }

    // Update is called once per frame
    void Update()
    {

    }
}
